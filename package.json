{
  "name": "cicumikuji",
  "packageManager": "pnpm@8.3.0",
  "version": "1.0.2",
  "description": "",
  "author": "Marco Solazzi",
  "license": "MIT",
  "private": true,
  "prettier": {
    "trailingComma": "all",
    "tabWidth": 2,
    "semi": true,
    "singleQuote": true
  },
  "scripts": {
    "web:start": "pnpm --filter=web run start",
    "web:build": "pnpm --filter=web run build",
    "functions:build": "pnpm --filter=functions run build",
    "setup-env": "bash ./scripts/init.sh"
  },
  "engines": {
    "node": ">=16.13.0",
    "pnpm": ">=7.13.4"
  },
  "devDependencies": {
    "firebase-tools": "12.4.5",
    "prettier": "3.0.0"
  }
}
